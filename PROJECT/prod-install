sudo aptitude install python-pip
sudo pip install django==1.5.1
sudo apt-get install libpq-dev python-dev
sudo pip install psycopg2

sudo pip install django-tastypie==0.11.1
sudo pip install django-guardian==1.2.4
sudo pip install django-celery==3.1.10

Setup crosser user (nologin)

install missing INSTALLED-APPS
install nginx & copy nginx config into sites-available
symlink config into sites-enabled
create postgres user
create postgres database
edit /crosser_web/settings.py for database + password
initialize database vi ./manage.py  (remember the default data fixtures!)
test
turn off DEBUG=true in settings.py
generate static files (./manage.py collectstatic)


rabbitmq:
  sudo aptitude install rabbitmq-server
  sudo rabbitmqctl
  sudo rabbitmqctl add_user rabbit c14h2002
  sudo rabbitmqctl add_vhost /django_crosser

R
install rScript! (duh)
sudo aptitude install r-base r-base-dev r-base-html
u
install libs
sudo R
install.packages("ggplot2")
install.packages("reshape")


copy the fastcgi.conf and celery.conf files from ../PROJECT/config to /etc/init/
Start them with sudo service celery start; sudo service fastcgi start;

install flup
sudo pip install flup==1.0.2



